Read and save json file using angular and api using .net web api. Backend (.NET Core API):

Reads from and writes to data.json

Provides endpoints like GET /api/observations and POST /api/observations

Frontend (Angular):

Summary View: shows data in a table

Detailed View: list of sampling times + dynamic form to edit and save
